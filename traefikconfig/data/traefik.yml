api:
  dashboard: true

entryPoints:
  web:
    address: :80

  websecure:
    address: :443

pilot:
  dashboard: false

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
  file:
    filename: /configurations/dynamic.yml